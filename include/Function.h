/*В классе Function задается функция, 
максимум или минимум которой необходимо найти с помощью данного алгоритма. */
class Function {
public:
    float Rozenbrock();
    float Shekel();
    float Ackley();
    float Rastrigin();
}